INSTRUCTIONS:

(JAVASCRIPT CODE)

In order to call the function you should pass two parameters: 

 * @numeros -> array of numbers
 * @suma -> target value

Example:

let numeros = [1, 9, 5, 0, 20, -4, 12, 16, 7];
let suma = 12;
const pairs = findPairs(numeros, suma)

in pairs array the numers who sum 'suma' will be storaged

You can print in a console to see the result:
Ej.
console.log(pairs);


